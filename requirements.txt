# Runtime dependencies (mirror of pyproject.toml [project.dependencies]).
# For development, prefer: pip install -e .[dev]

# intake — digital PDF text layer
pymupdf>=1.24
pdfplumber>=0.11

# intake — image / OCR lane
opencv-python-headless>=4.9
pytesseract>=0.3.10
Pillow>=10.0

# NLP / NER (also needs: python -m spacy download en_core_web_sm)
spacy>=3.7

# LLM seam (OpenAI-compatible client)
openai>=1.30

# graph store
neo4j>=5.20

# backend / API
fastapi>=0.110
uvicorn[standard]>=0.29

# config + corpus download
pydantic-settings>=2.2
python-dotenv>=1.0
requests>=2.31
